import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs/blocks'
import * as DndListStories from './dnd-list.stories'

<Meta of={DndListStories} />

# DndCardList

O `DndCardList` exibe uma lista de cards arrastáveis usando o DnD Kit.  
Ele espera um array de itens com `id` e `content`.

<Controls of={DndListStories.Default} />

---

## Exemplo de Uso

<Canvas>
  <Story of={DndListStories.Default} />
</Canvas>