import { Meta, Canvas, Story, Controls } from "@storybook/addon-docs/blocks"
import * as SeparatorStories from "./separator.stories"

<Meta of={SeparatorStories} />

# Separator

O **Separator** é um componente de interface utilizado dentro do design system para compor interfaces consistentes.

## Como funciona

Este componente encapsula estilos e comportamento reutilizável.

## Estrutura básica

```tsx
import { Separator } from "@/components/ui/separator"

export function Example() {
  return (
    <Separator>
      Conteúdo
    </Separator>
  )
}
```

## Exemplo básico

<Canvas>
  <Story of={SeparatorStories.Default} />
</Canvas>
