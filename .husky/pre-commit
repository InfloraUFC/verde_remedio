echo "Rodando lint..."

if [ "$BRANCH" = "main" ] || [ "$BRANCH" = "development" ]; then
  echo "Rodando lint antes do push ($BRANCH)..."
  pnpm run lint
else
  echo "Push sem lint (branch $BRANCH)"
fi